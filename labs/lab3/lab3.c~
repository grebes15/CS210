#include <stdio.h>

main(){

int x;
int y;
int z;

printf("Enter the two values to be added: ");
scanf("%d %d", &x, &y);
z = x + y;
printf("The sum of %d", x);
printf(" and %d", y);
printf(" is %d\n", z);
}
