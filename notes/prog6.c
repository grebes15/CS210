/* prog6.c -- more sophisticated output 
 * 
 * Compile using:
 *       gcc prog6.c -o prog6
 *
 * Execute using:
 *       ./prog6
 *
 * (Any name may be used as the executable; by default it is a.out)
 */

#include <stdio.h>

main() {
    int i = 10;
    printf("%d\n",i);
}
