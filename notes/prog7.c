/* Finally, the most famous c program in the world: 
 * 
 * Compile using:
 *       gcc prog7.c -o prog7
 *
 * Execute using:
 *       ./prog7
 *
 * (Any name may be used as the executable; by default it is a.out)
 */

#include <stdio.h>

main() {
    printf("hello, world!\n");
}
