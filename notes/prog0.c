/* prog0.c -- syntactically correct (empty body, does nothing)
 * 
 * Compile using:
 *       gcc prog0.c -o prog0
 *
 * Execute using:
 *       ./prog0
 *
 * (Any name may be used as the executable; by default it is a.out
 */

main() {
}
