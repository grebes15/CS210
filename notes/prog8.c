// prog8.c -- compile with "gcc -pedantic prog8.c"
// 
// Compile using:
//       gcc -pedantic prog8.c -o prog8
//
// (Any name may be used as the executable; by default it is a.out)

#include <stdio.h>

main() {
    printf("hello, world!\n");
    int i = 10;
}
